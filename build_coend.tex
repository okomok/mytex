\documentclass{article}
\usepackage{strd,cat,my}
\usepackage[active,tightpage]{preview}

\begin{document}

\myin{preview,math,aligned}{
    \myitem &
    \strdgathered{
        \strdmid, \strdrlabel{\coprod _ C F(C,C)},
        \strdcell{f},
        \strdmid, \strdrlabel{\coprod _{C,D} \mathcal{C}(D,C) \otimes F(C,D)}
    } \mydefeq
    \strdgathered{
        \strdmid, \strdrlabel{\coprod _ C F(C,C)},
        \strdcell{=},
        \strdmid, \strdrlabel{\coprod _ D F(D,D)},
        \strdbijframe{3}{3.2}{
            \strdmid,
            \strdcell{\scriptscriptstyle\coprod},
            \strdmid, \strdrlabel{F(D,D)},
            \strdroadlaside,
            \strdbijframe{2.3}{2.3}{
                \strdroad{\strdmid}, \strdroadlabel{F(C,D)}{F(D,D)},
                \strdcell{F(\texttt{-},D)},
                \strdroad{\strdright}, \strdroadlabel{D}{C}, \strdljump
            }, %\strdframellabel{\strdlvert{\operatorname{flip}}},
            \strdbranch{
                \strdrjump, \strdroad{\strdmid}%, \strdroadlabel{D}{C}
            },
            \strdxscale{1.2}\strdljump, \strdmid, \strdrlabel{F(C,D)}
        }, \strdframelabelat{Left EndCorner}{\coprod ^ {\strdrlap{C,D}}},
        \strdxscale{1.2}\strdrjump, \strdmid, \strdrlabel{\coprod _{C,D} \mathcal{C}(D,C) \otimes F(C,D)}
    } \\
    \myitem &
    \strdgathered{
        \strdmid, \strdrlabel{\coprod _ C F(C,C)},
        \strdcell{g},
        \strdmid, \strdrlabel{\coprod _{C,D} \mathcal{C}(D,C) \otimes F(C,D)}
    } \mydefeq
    \strdgathered{
        \strdmid, \strdrlabel{\coprod _ C F(C,C)},
        \strdbijframe{3}{3.2}{
            \strdmid,
            \strdcell{\scriptscriptstyle\coprod},
            \strdmid, \strdrlabel{F(C,C)},
            \strdroadlaside,
            \strdbijframe{2.3}{2.3}{
                \strdroad{\strdmid}, \strdroadlabel{F(C,D)}{F(C,C)},
                \strdcell{F(C,\texttt{+})},
                \strdroad{\strdright}, \strdroadlabel{D}{C}, \strdljump
            }, %\strdframellabel{\strdlvert{\operatorname{flip}}},
            \strdbranch{
                \strdrjump, \strdroad{\strdmid}%, \strdroadlabel{D}{C}
            },
            \strdxscale{1.2}\strdljump, \strdmid, \strdrlabel{F(C,D)}
        }, \strdframelabelat{Left EndCorner}{\coprod ^ {\strdrlap{C,D}}},
        \strdxscale{1.2}\strdrjump, \strdmid, \strdrlabel{\coprod _{C,D} \mathcal{C}(D,C) \otimes F(C,D)}
    } \\
}

\myin{preview,math,aligned}{
    \myitem &
    \strdgathered{
        \strdmid, \strdrlabel{\operatorname{Coeq}(f,g)},
        \strdcell{\operatorname{coeq}},
        \strdmid, \strdrlabel{\coprod _ C F(C,C)},
        \strdcell{\scriptscriptstyle\coprod},
        \strdmid, \strdrlabel{F(C,C)}
    } 
}

\end{document}
